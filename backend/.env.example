# Email (SMTP) configuration for Nodemailer
# Recommended: use a provider SMTP with an App Password (e.g., Gmail App Password) or a transactional service (SendGrid, Mailgun, Resend via SMTP)
# If you set SMTP_HOST, host/port/secure will be used; otherwise it falls back to service: 'gmail'

# Sender identity
MAIL_FROM="Jay's Blade and Snow <shanemelasuncion@gmail.com>"
EMAIL_USER=shanemelasuncion@gmail.com
EMAIL_PASS=your_app_password_here

# Explicit SMTP settings (optional; uncomment to override Gmail service defaults)
# For Gmail SMTP
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_SECURE=true
#
# Alternative (STARTTLS)
# SMTP_PORT=587
# SMTP_SECURE=false

# Optional SMTP tuning
# SMTP_MAX_CONNECTIONS=1
# SMTP_MAX_MESSAGES=50
# SMTP_CONNECTION_TIMEOUT=15000
# SMTP_GREETING_TIMEOUT=10000
# SMTP_SOCKET_TIMEOUT=20000
# SMTP_DEBUG=false

# Optional branding for emails
BRAND_NAME=brand_name
BRAND_LOGO_URL=https://your-cdn.example.com/path/to/logo.png
SUPPORT_EMAIL=support@example.com
SITE_URL=http://localhost:3000
BACKEND_URL=http://localhost:5000

# Uploads quota (to protect free-tier storage)
# Max total bytes allowed under backend/uploads/ before uploads are rejected.
# Default in code is 20971520 (20MB) if not set.
UPLOADS_MAX_BYTES=20971520
# Safety keep: minimum number of newest files to always preserve from deletion.
# Default in code is 10 if not set.
UPLOADS_KEEP_MIN=10

# Supabase configuration
# Replace with your actual project URL and service role or anon key in local .env (do not commit real secrets)
SUPABASE_URL=url
SUPABASE_KEY=YOUR_SUPABASE_API_KEY
SUPABASE_BUCKET=uploads
